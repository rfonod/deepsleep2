# DeepSleep 2.0 modern runtime dependencies (Python 3.8+).
# These are direct dependencies with conservative lower bounds; pip resolves the rest.
# To reproduce the original paper environment exactly, use requirements-legacy.txt instead.

# Core deep-learning stack
torch>=2.0
numpy>=1.23
scipy>=1.9
h5py>=3.7
scikit-learn>=1.1

# Data augmentation / visualization / progress
matplotlib>=3.6
seaborn>=0.12
pandas>=1.5
tqdm>=4.64

# Model summary (maintained replacement for torchsummary)
torchinfo>=1.8

# Notebook interface
jupyter>=1.0
